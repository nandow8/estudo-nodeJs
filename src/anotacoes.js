instalando nodemon no ubuntu (fica rodando aplicação llog igual ionic)
    npm install -g nodemon

    npm install --save-dev nodemon

    npm config get prefix

    nao usei essa -> set PATH=%PATH%;C:\Users\"Aqui seu usuario"\AppData\Roaming\npm;

instalando bodu body Parser   
    npm install body-parser --save   (quando fizer um post ele exibe quais valores estou salvando no banco)